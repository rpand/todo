import { renderComponent, expect } from '../test_helper';
import TaskList from '../../src/components/tast_list';

describe('Task list', =>{

  beforeEach(() => {
});

  it('it displays task component for each task', =>{
    expect();
  });

  it('it displays a placeholder if no tasks are displayed', =>{
    expect();
  });

  it('it displays the bar container', =>{
    expect();
  });

  it('it does not display tasks of the selected priority when the corresponding filter is on', =>{
    expect();
    expect();
    expect();
  });

  it('it sorts the tasks in the order selected when the corresponding sorting criteria is selected', =>{
    expect();
  });

  it('it moves tasks marked as done to the bottom of the task list', =>{
    expect();
  });

  it('', =>{
    expect();
  });

});
