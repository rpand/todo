import { renderComponent, expect } from '../test_helper';
import Task from '../../src/components/tast';

describe('Task', =>{

  beforeEach(() => {
});

  it('it displays the due date of the task in MM/DD/YYYY format', =>{
    expect();
  });

  it('it displays the priority of the task', =>{
    expect();
  });

  it('it displays the title of the task', =>{
    expect();
  });

  it('it displays the done status in a checkbox', =>{
    expect();
  });

  it('it toggles the done status when the checkbox is clicked', =>{
    expect();
  });

  it('it displays a button for editing', =>{
    expect();
  });

  it('it displays a button for deleting', =>{
    expect();
  });

});
