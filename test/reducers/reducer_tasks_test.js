import { expect } from '../test_helper';
import tasksReducer from '../../src/reducers/reducer_tasks';
import { * } from '../../src/actions/';

describe('Tasks Reducer', () => {
  it('handles actions with unknown type', () => {
    expect();
  });

  it('handles action of type FETCH_TASKS', () => {
    expect();
  });

  it('handles action of type ADD_TASK', () => {
    expect();
  });

  it('handles action of type DELETE_TASK', () => {
    expect();
  });

  it('handles action of type EDIT_TASK', () => {
    expect();
  });

  it('handles action of type FETCH_TASK', () => {
    expect();
  });

  it('handles action of type TOGGLE_COMPLETE', () => {
    expect();
  });

});
