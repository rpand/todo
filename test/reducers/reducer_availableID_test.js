import { expect } from '../test_helper';
import tasksReducer from '../../src/reducers/reducer_availableID';
import { * } from '../../src/actions/';

describe('Tasks Reducer', () => {
  it('handles actions with unknown type', () => {
    expect();
  });

  it('handles actions with INCREMENT_ID', () => {
    expect();
  });

  it('handles actions with FETCH_ID', () => {
    expect();
  });

});
